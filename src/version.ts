/**
 * Package identity constants. Kept in a single module so they can be imported
 * from both the runtime entry and the README/tests without touching the
 * generated package.json at runtime.
 */
export const name = '@fssou/camunda-mcp';
export const version = '0.1.0';
